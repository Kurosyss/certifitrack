# Options: 'deterministic' (direct offline PDF text stream extraction, default), 'gemini' (multimodal AI extraction)
CERTIFITRACK_PROVIDER=deterministic
GEMINI_API_KEY=

# Local Development Settings
PORT=3000
HOST=127.0.0.1
NODE_ENV=development
PUBLIC_BACKEND_URL=http://127.0.0.1:3000